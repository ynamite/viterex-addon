import { defineViterexConfig } from "./vite/viterex.js";

// Customize by passing overrides to defineViterexConfig; Vite's mergeConfig
// is used internally so you can override any block (plugins, build, server, ...).
export default defineViterexConfig({});
